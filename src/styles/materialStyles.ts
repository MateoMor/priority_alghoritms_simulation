import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
      primary: {
        main: '#1e293b', // slate-800 similar a tu header
      },
      secondary: {
        main: '#3b82f6', // blue-500
      },
    },
  });

export default theme;